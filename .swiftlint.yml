excluded:
 - Pods

line_length: 170
file_length:
  warning: 500
  error: 1200
identifier_name:
  excluded: # excluded via string array
    - id
disabled_rules:
    - type_body_length
    - identifier_name
    - line_length
    - trailing_whitespace
    - force_cast
    - force_try
